builder: dnf

dnf:
  releasever: 44
  exec: dnf5
  repodir: repodir/
  options:
    - --nogpgcheck
    - --skip-unavailable
    - --skip-broken
  packages:
    # Pipa kernel and firmware
    - kernel-pipa
    - kernel-pipa-modules
    - xiaomi-pipa-firmware

    # Boot and system
    - pipa-dracut
    - pipa-grub-config
    - pipa-metapkg
    - swclock-offset
    - bootmac

    # Audio and camera
    - alsa-ucm-conf-sm8250
    - pipa-sound-conf
    - libcamera
    - libcamera-ipa
    - libcamera-tools
    - gstreamer1-plugin-libcamera

    # Sensors
    - pipa-sensors
    - hexagonrpc
    - libssc
    - iio-sensor-proxy

    # Qualcomm firmware services
    - qrtr
    - rmtfs
    - tqftpserv
    - pd-mapper

    # GPU, display, wireless
    - mesa-dri-drivers
    - mesa-vulkan-drivers
    - libglvnd-glx
    - wireless-regdb
    - bluez

    # Power management
    - tuned
    - tuned-ppd

  exclude:
    - kernel
    - kernel-core
    - kernel-devel*
    - kernel-modules*
